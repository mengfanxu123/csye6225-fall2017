language: java
jdk:
  - oraclejdk8

sudo: true
install: true

before_script:
 - chmod +x gradlew
 
script:
  - ./gradlew war

notifications:
  email:
    recipients:
      - t.parikh@northeastern.edu
